$(function(){ $("#slides").slides(); });

(function($) {
	var rollingParam = {};
	
	$.Rolling = {
		defaults : {
			id : null,
			motion : "start",
			delayTime : 3000,
			intervalTime : 1000,
			displayTimer : null,
			timer : null,
			timer2 : null,
			timer3 : null
		}
			
	};
	
	$.fn.exted({
		Rolling : function (options) {
			var id = $(this).prop("id");
			
		}
	});
})
    
 